# VFS and Filesystems

# VFS Layer
- inode
- dentry
- file descriptor table

# open("foo.txt") → ext4 inode kernel path

# ext4 Journaling
- JBD2
- ordered mode (default)

# Why ordered mode prevents metadata-data inconsistency
